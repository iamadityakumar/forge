# Copy to .env and adjust. The app is currently in-memory and does not
# require a database, but DATABASE_URL is wired through for when the
# Postgres-backed store is enabled.
DATABASE_URL=postgres://postgres:secret@postgres:5432/forge?sslmode=disable

# Used by Caddy / DuckDNS (Step 6).
DUCKDNS_TOKEN= 
