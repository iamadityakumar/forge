# Caddy reverse proxy for Forge (Step 6 — HTTPS via DuckDNS).
# Caddy obtains and renews the TLS certificate automatically.
# Uses HTTP-01 challenge (DuckDNS record must point to this server).

{
	# Email for ACME / Let's Encrypt registration.
	email iamadityakumar@users.noreply.github.com
}

4orge.duckdns.org {
	reverse_proxy forge-orchestrator:8080
}
