# Two Pointers Strategy

Use two pointers on sorted arrays or sequences to find pairs or sub-segments satisfying conditions in O(N).

## Common Patterns
- Left and Right pointers moving towards middle (e.g. 2-Sum on sorted array).
- Slow and Fast pointers (e.g. Cycle detection, middle element).